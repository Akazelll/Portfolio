import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['iili.io', 'akazellapp.netlify.app', ''], // Daftarkan domain gambar eksternal yang Anda gunakan
  },
};

export default nextConfig;
